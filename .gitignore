# Secrets / local config — never commit
ibkr.input

# Trade journal data — local, per-user, not source
data/trade_journal.jsonl

# Emotional circuit breaker state — local, per-user, not source
data/circuit_breaker_state.json

# Win-streak throttle state — local, per-user, not source
data/win_streak_state.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Preliminary prototype environment
preliminary/.venv/
preliminary/__pycache__/

# OS
.DS_Store

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
.idea